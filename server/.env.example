PORT=5000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=sitedashboard
# Set DB_SSL=true for Azure Database for PostgreSQL
DB_SSL=false

# Comma-separated frontend origins for production CORS (empty = allow all)
CORS_ORIGINS=
