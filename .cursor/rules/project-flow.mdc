---
description: Live build-flow map for Site Ops Dashboard — keep updated on every change
alwaysApply: true
---

# Project Flow (live)

> **Agent rule:** After any meaningful change in this repo, update the Status column below (`Done` / `Next` / `Todo`). Keep this file the single crisp map of progress. Also sync `PROJECT_CHECKLIST.md` checkboxes.

## Goal
Site Ops Dashboard → manage Sites + Installations + Summary → React + Express + Postgres → Azure deploy.

## Flow map

| Step | What | Status |
|------|------|--------|
| 0.1 | Azure account + credit | Done |
| 0.2 | Install PostgreSQL local (5432) | Done |
| 0.3 | Create DB `sitedashboard` | Done |
| 0.4 | Tables: Sites → Installations (FKs) | Done |
| 1.1 | `/server` + `npm init` + deps | Done |
| 1.2 | `.env` (DB_* + PORT) | Done |
| 1.3 | `db.js` pool + `server.js` (cors/json/morgan) | Done |
| 1.4 | Boot test (DB connect + `/health`) | Done |
| 2 | Sites CRUD APIs + error middleware | Done |
| 3 | Installations CRUD APIs (+ site join) | Done |
| 4 | `GET /api/summary` aggregations | Done |
| 5 | Manual API tests (curl/Postman) | Done |
| 6 | `/client` Vite React + routing | Done |
| 7 | Dashboard summary cards | Done |
| 8 | Sites page (list/search/filter/CRUD) | Done |
| 9 | Installations page (list/CRUD) | Done |
| 10 | Polish (responsive, loading/errors) | Done |
| 10.1 | List pagination (max 20) + sites search debounce | Done |
| 10.2 | Sites/Installations XLSX import (upsert by name) + export | Done |
| 11 | Azure Postgres + App Service + SWA | Done |
| 12 | README + clean Git history + push | **Next** |

## Chat slices
| Chat | Covers | When to new chat |
|------|--------|------------------|
| A | Steps 0–1 | Done — use new chat |
| B | Steps 2–5 | Done — use new chat |
| C | Steps 6–7 | Done — use new chat |
| D | Steps 8–9 | Done — use new chat |
| E | Steps 10–12 | **Current next** |

## Schema (quick)
`Sites(id,name,location,status)` · `Installations(site_id→Sites, equipment_type, status, notes)`

## Stack paths
`/server` Express API · `/client` React · Azure: `siteops-rg` (Postgres + App Service + SWA)

## Resume
Read this + `PROJECT_CHECKLIST.md` → first **Next/Todo** / first `- [ ]` → implement → mark Done → update both files.
